#version 450
layout(local_size_x = 64) in;

// Storage buffer for integers
layout(set = 0, binding = 0) buffer Data {
    int values[];
};

void main() {
    uint idx = gl_GlobalInvocationID.x;
    values[idx] *= values[idx];
}
